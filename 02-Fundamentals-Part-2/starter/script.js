// activating strict mode
// making sure that the code is running in strict mode
'use strict';

let hasDriversLicense = false;
const passTest = true;

// catches error
if (passTest) hasDriverLicense = true;

if (hasDriversLicense) console.log('You are allowed to drive!');