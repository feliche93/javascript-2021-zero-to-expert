// strings and TemplateLiterals

const firstName = 'Felix';
const job = 'Data Engineer'
const birthYear = 1993;

const text = `${firstName} is a ${job} and was born in ${birthYear}`
console.log(text);

console.log(`This is a multi line string
is it working?`);